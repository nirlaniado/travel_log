# Copy to secrets.auto.tfvars (gitignored, auto-loaded by terraform) and fill
# in real values. NEVER commit secrets.auto.tfvars. These map 1:1 to
# ../charts/travellog/secrets.yaml and ../.env.example.
mysql_user          = "travel"
mysql_password      = "CHANGE_ME"
mysql_root_password = "CHANGE_ME"

# Required only for the EKS observability stack (enable_eks + enable_observability)
grafana_admin_password = "CHANGE_ME"
